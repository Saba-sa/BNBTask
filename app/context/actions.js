export const setProvider = (provider) => ({
    type: 'SET_PROVIDER',
    payload: provider,
  });
  
  export const setSigner = (signer) => ({
    type: 'SET_SIGNER',
    payload: signer,
  });
  
  export const setAccount = (account) => ({
    type: 'SET_ACCOUNT',
    payload: account,
  });
  export const setDeploymentTimestamp = (account) => ({
    type: 'SET_ACCOUNT',
    payload: account,
  });
  
  export const setContract = (contract) => ({
    type: 'SET_CONTRACT',
    payload: contract,
  });
  
  export const setBalance = (balance) => ({
    type: 'SET_BALANCE',
    payload: balance,
  });
  